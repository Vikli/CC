<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qbase.skipper.q_base">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity android:name=".barcode_scan" />
        <activity android:name=".table_redactor" />
        <activity android:name=".inst_db" />
        <activity android:name=".construct_db" />
        <activity android:name=".search_by_code" />
        <activity android:name=".profile_act" />
        <activity android:name=".user_authorization" />
        <activity android:name=".give_or_pick" />
        <activity android:name=".give" />
        <activity android:name=".pick"></activity>
    </application>

</manifest>